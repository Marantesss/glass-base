const { contract } = require('./../models')

const contractController = {}

contractController.getOne = async (req, res) => {

};

contractController.getAll = async (req, res) => {

};

module.exports = contractController;
